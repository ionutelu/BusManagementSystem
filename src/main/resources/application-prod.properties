# ?? Prod Profile ????????????????????????????????????????????????????????????
# Activated via SPRING_PROFILES_ACTIVE=prod environment variable in deployment.
# No debug output; schema must already exist (Flyway manages it).

spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=validate

# JWT_SECRET environment variable MUST be set in production.
# Generate a strong random key: openssl rand -base64 32
# jwt.secret is read from environment variable JWT_SECRET (no default safe for prod)


